---
name: researchcraft-notebook
description: >-
  Keep ResearchCraft's living lab notebook with the notebook tool: hypotheses,
  methods, observations, decisions, artifact links, and append-only corrections.
---

# ResearchCraft lab notebook

You are on DeepSeek Harness. Use the `notebook` tool as you work.

- `action: "log"` (default) for a hypothesis, method, observation, decision, or note.
- Attach `artifacts` for figures, tables, and scripts.
- Thread with `relatesTo` and `stance`. Correct an entry with a new log that sets `supersedes`.
- `action: "read"` to recall ids and earlier findings in this session.
