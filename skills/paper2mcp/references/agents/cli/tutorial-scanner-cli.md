# CLI tool scanner

Follow the [shared role responsibilities](../tutorial-scanner.md), [selection and wrapping rules](../../tool-selection-and-wrapping.md), and [CLI route](../../routes/cli.md). Use the same handoff/report contracts and independent-agent requirements.

Inspect documented commands, argument definitions, README/wiki examples, and official tests. Bind supported executable/subcommand sequences and native inputs/outputs. Do not assume --help exists. Distinguish independently useful operations from setup commands; merge duplicate examples. All selected examples map to the cli_wrapper module.
