# R tool scanner

Follow the [shared role responsibilities](../tutorial-scanner.md), [selection and wrapping rules](../../tool-selection-and-wrapping.md), and [R route](../../routes/r.md). Use the same handoff/report contracts and independent-agent requirements.

Inspect exported package functions, vignettes, R Markdown/Quarto, scripts, and official examples/tests. Bind actual R symbols and source paths. One public function can be a valuable tool. Use the shared exact filter semantics and record native R fixture/data requirements. Keep one owning source module for each selected tool; multiple examples may support it.
